import { login } from './actions';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="login"><div className="loginbox card"><div className="brand" style={{border:0,padding:0,marginBottom:18}}><div className="brandmark">RF</div><div><h1 style={{color:'#0f172a',fontSize:20}}>Rajguru Foods ERP</h1><p>Secure inventory, warehouse and pledge control</p></div></div>{q.error&&<div className="notice" style={{background:'#fee2e2',borderColor:'#fecaca',color:'#991b1b',marginBottom:12}}>{q.error}</div>}<form action={login} className="grid"><div className="field"><label>Email</label><input className="input" type="email" name="email" required/></div><div className="field"><label>Password</label><input className="input" type="password" name="password" required/></div><button className="btn primary">Sign in</button></form><p className="muted" style={{fontSize:10,marginTop:14}}>Accounts are created by the ERP administrator through Supabase Auth.</p></div></main>}
